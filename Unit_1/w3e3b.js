function calcCircumference(radius) {
    document.write("La circunferencia vale " + (2*radius*Math.PI) + "<br>");
}

function calcArea(radius) {
    document.write("El area vale " + (Math.PI*radius^2) + "<br>");
}

calcCircumference(3);
calcArea(6);